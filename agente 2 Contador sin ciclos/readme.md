# AGENTE
*En este archivo se crea el objeto de la clase Comportamiento, el metodo setup() en el cual se encuentra el metodo addBehaviour()
donde se manda como parametro el objeto creado*

# COMPORTAMIENTO

*Aquí se realizaron los metodos action() y done(), en action() se realiza la impreción e incrementación del contador, en done() se analiza si ya 
es 100, en dado caso de que no se así retorna un false regresando al action(), en el caso de que sea cierto retorna un true y ahi termina*
